import React, {
    Component
} from 'react';
import { Toolbar } from './toolbar';

class Dashboard extends Component {
  render() {
    return (
      <Toolbar />
    );
  }
}

module.exports = {
  Dashboard
}
